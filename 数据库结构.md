# Neo4j AuraDB 数据库结构与 API 说明

> 审计时间：2026-07-26 19:40（Asia/Shanghai）
> 数据来源：当前 `supply-chain-api/.env` 连接的 Neo4j AuraDB 实例  
> 注意：本文区分“AuraDB 已有数据”和“工作区中已配置但尚未持续写入的外部 API”。
> 阶段 3—10 的详细映射见 `docs/unified_neo4j_model.md`、`docs/provider_risk_recalculation.md`、`docs/stage5_gdelt_weather_mapping.md`、`docs/stage6_geospatial_risk_zones.md`、`docs/aisstream_port_traffic.md`、`docs/route_recommendation.md`、`docs/stage9_test_validation.md` 和 `docs/十阶段项目更新总结.md`。

## 1. 数据库总览

| 指标 | 当前值 |
|---|---:|
| 节点总数 | 24,164 |
| 关系总数 | 47,924 |
| 节点标签种类 | 75 |
| 关系类型种类 | 110 |
| 约束 | 84 |
| 索引 | 121 |

这些数量来自 `2026-07-26T11:40:05Z` 的只读快照。GitHub Actions 会继续写入 GDELT 和 Open-Meteo 观测，因此节点和关系数量会动态增长；复核当前值应重新运行 `python scripts/audit_current_backend.py`。

当前图谱不是单一的物流路线图，而是由以下几个子图叠加组成：

1. 供应商、零部件、车型和工厂网络。
2. 跨境仓库、港口、机场、铁路站和市场网络。
3. `RouteSegment` 成本、时效、距离和综合风险子图。
4. 生产计划、库存、销售订单和交付记录子图。
5. 国家、港口、外部事件和实时观测风险子图。
6. AISStream.io 船舶、船位观测和运单跟踪子图。

### 1.1 阶段 3 统一模型

阶段 3 不删除或改名旧实体，而是增加规范标签和兼容关系。当前主要统一标签数量如下：

| 规范标签 | 数量 | 统一主键 |
|---|---:|---|
| `TransportLocation` | 102 | `location_id` |
| `Warehouse` | 42 | `warehouse_id` |
| `RouteSegment` | 613 | `segment_id` |
| `Route` | 28 | `route_id` |
| `GeoZone` | 10 | `zone_id` |
| `RiskObservation` | 21,475 | `observation_id` |
| `CostObservation` | 609 | `observation_id` |
| `DelayObservation` | 628 | `observation_id` |
| `Evidence` | 12,181 | `evidence_id` |
| `PortTrafficSnapshot` | 0 | `snapshot_id`，等待真实 AIS 聚合 |
| `RecommendationSnapshot` | 3 | `snapshot_id`；阶段 8 AuraDB 冒烟调用快照，30 天 TTL |

旧标签仍然有效，例如 `RouteLeg`、`RiskSnapshot`、`CostEstimate`、`NewsRiskEvent` 和 `WeatherRiskSnapshot`，因此旧接口可以继续查询。统一迁移没有删除任何节点或关系。

阶段 4 完成后，613 个 `RouteSegment` 均已写入 `provider_risk_status` 和 `provider_risk_data_completeness`。当前只有 18 个路段具备未过期 Open-Meteo 风险，GDELT 有效路线风险为 0，AIS 有效快照为 0；分数只来自有效期内的真实 Provider，过期后自动回到不可用。

### 1.2 阶段 5 新闻聚类与路线天气

- 当前有 12,202 个原始 `NewsRiskEvent`，定时更新继续保留可审计文章元数据。
- 当前有 9,166 个 `NewsRiskCluster`；同一新闻事件簇只参与一次评分。
- 当前有 36 个 `RouteWeatherRiskSnapshot`，18 条路段具有未过期 Open-Meteo 风险。
- 路线天气优先沿几何采样；估算几何明确标记 `partial`，不会冒充运营轨迹。
- 101 条合成路线、8 条失效估算路线、2 条地理不可行路线未附加真实天气风险。
- GitHub Actions 会继续更新 GDELT；审计时 10 个区域已有最新抓取记录，但路线 GDELT 风险均已超过有效期，状态会随 TTL 动态变化。

### 1.3 阶段 6 地理映射

- 102 个 `TransportLocation` 均有坐标审计状态：33 个 `reference`、56 个 `estimated`、13 个 `unavailable`。
- 10 个 `GeoZone` 均有有效 GeoJSON 和来源字段。
- 613 个 `RouteSegment` 均有 `geometry_status`；5 条海运、5 条公路有估算网络几何，5 条铁路保留端点回退，4 条跨洋公路/铁路被判定无效，594 条 synthetic 路段不提升为真实几何。
- `PASSES_THROUGH` 共 69 条：65 条有效（16 条几何交叉、49 条低置信度端点推断），4 条无效跨洋路线关系已软停用。
- 最终重复 dry-run 的地点、路段、风险区和关系更新数均为 0。

### 1.4 阶段 8 推荐快照

- 新增 3 个 `RecommendationSnapshot`：2 个开发期 `v1.0` 和 1 个最终验证 `route-recommendation-v1.1`，均为 `data_status=computed`；
- 新增 12 条 `(RouteSegment)-[:INCLUDED_IN]->(RecommendationSnapshot)` 关系；
- 快照保存完整请求、实际权重、硬约束、评分版本、响应 JSON 和路线 ID；
- 快照是算法审计记录，不是运输 Provider 观测，不参与风险加权；
- 本阶段删除节点 0、删除关系 0。

### 1.5 阶段 9 只读数据验证

- 当前代码评分版本为 `route-recommendation-v1.2`，阶段 8 已保存的 3 个快照仍保留其原始 `v1.0/v1.1` 版本用于审计；
- AuraDB 只读验证时共有 24,135 个节点、47,866 条关系，本阶段新增、修改、删除数据库数据均为 0；
- 613 个 `RouteSegment` 没有重复业务 ID，18 个带当前 Provider 风险值的分段均有 Provider；
- 102 个 `TransportLocation` 均有名称和 `coordinate_status`，其中 89 个有经纬度；
- 29 个供应商中 25 个有 `SHIPS_FROM`，其余 4 个会被推荐接口明确拒绝，不会猜测起点；
- Open-Meteo 当前有 18 个未过期风险分段；GDELT 当前有效分段为 0，AIS 快照为 0，因此两者不会生成伪造风险；
- 机器可读结果见 `artifacts/stage9_data_validation_20260726T110331Z.json`。

## 2. 主要节点结构

### 2.1 路线、成本与风险

| 标签 | 数量 | 用途 | 关键字段 |
|---|---:|---|---|
| `RouteSegment` | 613 | 可计算的有向运输边，包含 19 个兼容 `RouteLeg` | `segment_id`, `canonical_mode`, `data_status`, `feasibility_status`, `geometry_geojson`, `geometry_source`, `geometry_status`, `geometry_confidence`, `provider_risk_*` |
| `RouteCostObservation` | 586 | 路段成本观测 | 成本值、观测时间、数据源 |
| `RouteDelayObservation` | 628 | 延误与时效观测 | 延误值、观测时间、数据源 |
| `RiskFactor` | 0 | 阶段 4 已删除全部 612 个无 Provider、无 Evidence 的合成/派生风险节点 | 新风险直接记录在 `RouteSegment.provider_risk_*`，并保留 Provider 与 Evidence |
| `RiskEvent` | 20 | 港口拥堵、罢工、冲突、运力短缺等事件 | `eventId`, `eventType`, `severity`, `affectedRegion`, `startDate`, `endDate` |
| `Route` | 28 | 预定义路线与兼容 `VehicleRoute` | `route_id`, `data_status`, `source`, `provider`, `scoring_version`, `validity_status` |
| `RouteRecommendation` | 15 | 历史预计算推荐结果 | `recommendationId`, `originName`, `destinationName`, `rank`, `pathNodeNames` |
| `RiskMatrixCategory` | 4 | 成本-风险矩阵分类 | 低成本/高成本、低风险/高风险 |

核心路段结构：

```text
(fromNode)<-[:FROM_NODE]-(segment:RouteSegment)-[:TO_NODE]->(toNode)
(segment)-[:USES_MODE]->(mode:TransportMode)
(segment)-[:HAS_COST_OBSERVATION]->(cost:RouteCostObservation)
(segment)-[:HAS_DELAY_OBSERVATION]->(delay:RouteDelayObservation)
(segment)-[:HAS_RISK_OBSERVATION]->(observation:RiskObservation)
(segment)-[:PASSES_THROUGH]->(zone:GeoZone)
(segment)-[:EXPOSED_TO_NEWS_RISK]->(zone:NewsRiskZone)
(segment)-[:EXPOSED_TO_NEWS_CLUSTER]->(cluster:NewsRiskCluster)
(segment)-[:HAS_ROUTE_WEATHER_SNAPSHOT]->(weather:RouteWeatherRiskSnapshot)
(segment)-[:CLASSIFIED_AS]->(matrix:RiskMatrixCategory)
(route:Route)-[:HAS_SEGMENT]->(segment)
(segment)-[:INCLUDED_IN]->(recommendation:RecommendationSnapshot)
```

### 2.2 供应链与汽车产品

| 标签 | 数量 | 用途 |
|---|---:|---|
| `Supplier` | 29 | 供应商，包含 CATL、BYD Battery、Bosch、TSMC Automotive 等 |
| `AutoPart` | 41 | 零部件主实体 |
| `Component` | 41 | 零部件兼容标签 |
| `Part` | 40 | 零部件兼容标签 |
| `VehicleModel` / `CarModel` / `Product` | 各 15 | 车型/产品的兼容标签 |
| `VehicleSystem` / `Feature` | 各 18 | 车辆系统与功能 |
| `Factory` | 19 | 生产工厂 |
| `OEM` | 5 | 整车厂商 |

主要业务链：

```text
(VehicleModel)-[:HAS_SYSTEM]->(VehicleSystem)
(VehicleSystem)-[:REQUIRES_PART]->(AutoPart)
(AutoPart)-[:SUPPLIED_BY|IS_SUPPLIED_BY]->(Supplier)
(Supplier)-[:SUPPLIES_TO_FACTORY]->(Factory)
(Factory)-[:PRODUCES]->(VehicleModel)
(Supplier)-[:SHIPS_FROM]->(EntryPoint)
```

### 2.3 物流节点

| 标签 | 数量 |
|---|---:|
| `TransportLocation` | 102 |
| `Port` | 20 |
| `Airport` | 16 |
| `TransitHub` | 11 |
| `ArrivalWarehouse` | 10 |
| `OverseasWarehouse` | 10 |
| `Destination` | 10 |
| `DestinationCountry` | 10 |
| `ExportWarehouse` | 8 |
| `EntryPoint` | 8 |
| `DepartureWarehouse` | 8 |
| `DeparturePoint` | 8 |
| `TransferPoint` | 8 |
| `Warehouse` | 42 |
| `RailTerminal` | 5 |
| `TransportMode` | 7 |

运输关系包括 `TRUCK_ROUTE`、`RAIL_ROUTE`、`SEA_ROUTE`、`AIR_ROUTE`、`CONNECTS_TO`、`TRANSPORT`、`RECEPTION`、`DEPARTURE` 和 `DELIVERY`。

### 2.4 生产计划与交付

| 标签 | 数量 |
|---|---:|
| `ProductionPlan` | 36 |
| `InventoryRecord` | 48 |
| `SalesOrder` | 36 |
| `DeliveryRecord` | 36 |
| `FactoryIssue` | 12 |
| `TimePeriod` | 12 |
| `Market` | 6 |

主要关系包括 `HAS_PRODUCTION_PLAN`、`HAS_INVENTORY`、`PLACES_ORDER`、`FULFILLED_BY`、`DELIVERED_TO`、`TARGET_MARKET`、`AFFECTS_FACTORY` 和 `AFFECTS_PLAN`。

### 2.5 风险与外部观测

| 标签 | 数量 | 用途 |
|---|---:|---|
| `Country` | 17 | 国家贸易、地缘、制裁和冲突风险 |
| `CountryRiskObservation` | 17 | 国家风险观测 |
| `PortObservation` | 107 | 港口天气、拥堵和公开事件观测 |
| `SourceEvidence` | 11 | 外部证据和来源链接 |
| `IntegratedRiskAssessment` | 2 | 综合风险评估 |
| `IntegratedCostAssessment` | 2 | 综合成本评估 |

## 3. AISStream.io 船舶观测结构

### 3.1 AuraDB 当前实际数据

| 标签 | 数量 | 当前情况 |
|---|---:|---|
| `AisObservationTarget` | 4 | 上海、新加坡、鹿特丹和苏伊士监测区已通过 `MERGE` 建立 |
| `AisProviderState` | 1 | 当前 `configured=false`、`connected=false`，不含 API Key |
| `Vessel` | 1 | 旧 `DEMO CONTAINER` 保留审计，但已标记 `synthetic + excluded_from_risk` |
| `VesselObservation` | 1 | 旧示例位置同样标记为 `synthetic + excluded_from_risk` |
| `PortTrafficSnapshot` | 0 | 等待真实 AIS worker 产生观测窗口，不生成测试快照 |

迁移执行于 2026-07-26。当前没有真实 AIS 观测，因此 `observed_vessels=0`、`active_snapshots=0` 是正确状态；系统不会把旧示例船或“没有消息”解释为港口低风险。

真实 worker 写入的 `Vessel` 最新状态字段：

```text
mmsi
imo
vessel_name / name
vessel_type
latitude / longitude
speed_knots
course_degrees
heading_degrees
destination
draught_m
navigational_status_code / navigational_status
call_sign
last_ais_observed_at
last_static_observed_at
last_message_received_at
provider / source / source_type
data_status / status / excluded_from_risk
```

每艘船只保留一个覆盖更新的最新观测 `ais-latest:{mmsi}`：

```text
observation_id
mmsi
observed_at
received_at
position_observed_at / static_observed_at
latitude / longitude
speed_knots / course_degrees / heading_degrees
imo / vessel_name / vessel_type / destination / draught_m
navigational_status_code / navigational_status
raw_payload_hash
raw_payload_retained=false
```

系统不保存 `raw_payload_json`，也不为每一条高频坐标创建长期节点。`raw_payload_hash` 只用于重复消息和审计。

窗口聚合 `PortTrafficSnapshot` 字段：

```text
snapshot_id / target_id
window_start / window_end
vessel_count / anchored_count / average_speed
arrival_count / departure_count
congestion_score
observation_count
speed_coverage / navigation_status_coverage / temporal_coverage
confidence / data_completeness
observed_at / generated_at / expires_at
provider='AISStream.io'
source_type='ais_observed'
data_status='estimated'
calculation_status='derived_from_observed_ais'
```

AIS 关系结构现在是：

```text
(AisObservationTarget)-[:REPRESENTS_PORT]->(Port)
(AisObservationTarget)-[:REPRESENTS_ZONE]->(GeoZone)
(AisObservationTarget)-[:HAS_TRAFFIC_SNAPSHOT]->(PortTrafficSnapshot)
(Vessel)-[:HAS_OBSERVATION]->(VesselObservation)
(Vessel)-[:HAS_LATEST_OBSERVATION]->(VesselObservation)
(VesselObservation)-[:OBSERVED_IN_AIS_TARGET]->(AisObservationTarget)
(Port)-[:HAS_TRAFFIC_SNAPSHOT]->(PortTrafficSnapshot)
(GeoZone)-[:HAS_TRAFFIC_SNAPSHOT]->(PortTrafficSnapshot)
(RouteSegment)-[:EXPOSED_TO_AIS_TRAFFIC]->(PortTrafficSnapshot)
```

只有真实、未过期、带 `AISStream.io` Provider 和快照证据的 `congestion_score` 会映射到海运 `port_congestion`。缺少 AIS 时该因子为 `null/unavailable`，不得写入 `0.5/50` 默认风险；AIS 不进入铁路、公路或空运风险。

### 3.2 AISStream.io 当前代码订阅范围

工作区 `config/ais_observation_targets.json` 当前只配置需求指定的 4 个区域。订阅框比聚合框略大，用于观察到港/离港状态变化：

| 区域 | 图关联 | 聚合纬度/经度 | 订阅纬度/经度 |
|---|---|---|---|
| 上海港 | `Port CN-SHA/CNSHA` | `30.75,121.20` - `31.55,122.35` | `30.55,120.95` - `31.80,122.55` |
| 新加坡港 | `Port SGSIN` | `0.95,103.45` - `1.55,104.15` | `0.75,103.25` - `1.75,104.35` |
| 鹿特丹港 | `Port NLRTM` | `51.72,3.65` - `52.15,4.65` | `51.50,3.40` - `52.35,5.00` |
| 苏伊士运河 | `GeoZone red-sea` | `29.75,32.15` - `31.40,32.75` | `29.50,31.95` - `31.60,32.95` |

订阅不设置 `FiltersShipMMSI`，因此在不知道船舶 MMSI 时也能采集。当前过滤消息类型包括：

```text
PositionReport
StandardClassBPositionReport
ExtendedClassBPositionReport
LongRangeAisBroadcastMessage
ShipStaticData
StaticDataReport
```

常驻入口是 `scripts/run_ais_consumer.py`。FastAPI Web Service 不会自动启动该脚本；部署和排错见 `docs/aisstream_port_traffic.md`。

## 4. 当前已记录的外部数据 API

| 外部数据源 | 用途 | AuraDB 记录量/状态 |
|---|---|---|
| AISStream.io WebSocket | 船位、MMSI、IMO、船名、船型、速度、航向、目的地、吃水、导航状态和港区窗口聚合 | 消费器与 4 个监测目标已完成；`PortTrafficSnapshot=0`，旧示例观测已排除 |
| Open-Meteo Forecast / Marine API | 港口和路线天气、海况风险 | `WeatherRiskSnapshot=63`、`RouteWeatherRiskSnapshot=36`，其中 18 条路线风险未过期 |
| GDELT DOC 2.0 API | 冲突、制裁、关税、罢工、中断、事故和灾害等公开新闻 | `NewsRiskEvent=12,202`、`NewsRiskCluster=9,166`、`NewsRiskZone=10`；当前有效路线风险为 0 |
| SEC EDGAR / 历史公开参考 | 供应商、组件、工厂和路线参考证据 | 属于静态参考或历史导入，不是实时运输服务 Provider |
| Public Risk Crawler / 旧实验数据 | 旧风险和延误参考 | 多数属于历史或合成数据，不应直接当作实时风险 |

阶段 6 还使用 UN/LOCODE、OurAirports、GeoNames、Natural Earth、Marine Regions、OSRM/OpenStreetMap 和 searoute-py 建立静态参考坐标与几何。它们不是每小时 API；来源、许可证和真实/估算边界见 `docs/data_sources.md`。

另有 `wey-gu/supplychain-dataset-gen`、`external_repos_reference_fields`、`synthetic_cross_border_transport_risk` 和 `synthetic_supplygraph_style` 等上游仓库或合成实验数据，它们不是实时外部 API。

## 5. FastAPI 对外 HTTP 接口

当前 OpenAPI 公开 50 个 HTTP 操作。前端业务查询主要使用只读 `GET`，推荐主入口使用 `POST`；带管理员 Token 的更新入口也使用 `POST`。Swagger 地址：`/docs`。

### 5.1 服务与健康检查

| 接口 | 用途 |
|---|---|
| `GET /health/ais` | AIS worker 连接、最近消息、最近快照和错误状态；永不返回 Key |
| `GET /api/providers/status` | 前端统一读取 AISStream.io、GDELT 和 Open-Meteo 当前状态 |
| `GET /` | 服务基本信息 |
| `GET /health` | FastAPI 进程和数据库配置状态 |
| `GET /health/aura` | AuraDB 连通性检查 |

### 5.2 图谱概览

| 接口 | 主要参数 | 用途 |
|---|---|---|
| `GET /api/graph/summary` | 无 | 标签和关系数量统计 |
| `GET /api/supply-chain/routes` | `limit` | 返回原始路段，主要用于调试 |
| `GET /api/risk/overview` | `limit` | 国家、港口和路段风险概览 |

### 5.3 成本与风险

| 接口 | 主要参数 | 用途 |
|---|---|---|
| `GET /api/risk/segments` | `limit`, `minimum_risk` | 按综合风险排名路段 |
| `GET /api/cost/segments` | `order`, `limit` | 按成本升序或降序排名路段 |

### 5.4 路径规划

| 接口 | 主要参数 | 用途 |
|---|---|---|
| `GET /api/suppliers` | `search`, `limit` | 供应商下拉列表 |
| `GET /api/suppliers/{supplier_id}/origins` | 供应商 ID | 该供应商通过 `SHIPS_FROM` 明确关联的发货起点 |
| `GET /api/cities` | `search`, `limit` | 起终点城市/节点下拉列表 |
| `GET /api/routes/nodes` | `search`, `limit` | 返回可传给最短路算法的 `node_id` |
| `POST /api/routes/recommend` | JSON：供应商、起终点、货物、策略、权重、约束 | **前端主接口**：先应用硬约束，再按风险/成本/时效稳定排序 |
| `GET /api/routes/recommend` | 查询参数 | 旧前端兼容接口，OpenAPI 已标记 deprecated |
| `GET /api/recommendations/{snapshot_id}` | 推荐快照 ID | 回读完整请求结果 |
| `GET /api/routes/{route_id}` | POST 响应中的路线 ID | 从最近推荐快照回读单条路线 |
| `GET /api/routes/optimize` | `origin_id`, `destination_id`, `objective`, `risk_weight` | 单条最低成本、最低风险或平衡路径 |
| `GET /api/routes/recommendations` | `objective`, `risk_weight`, `limit` | 对数据库中预定义的 `Route` 排名 |

前端主调用示例：

```json
POST /api/routes/recommend
{
  "supplierId": "SUP-CATL",
  "origin": "Shanghai",
  "destination": "Hamburg",
  "cargo": {"type": "finished_vehicle", "vehicleType": "electric_vehicle", "quantity": 1},
  "strategy": "balanced",
  "weights": {"risk": 0.5, "cost": 0.3, "duration": 0.2},
  "constraints": {"allowedModes": ["road", "rail", "sea"]},
  "limit": 5
}
```

响应中每条路线包含：

```text
id, rank, name, riskScore, riskStatus, riskDataCompleteness,
cost, durationDays, distanceKm, finalScore, uncertaintyPenalty,
scoreBreakdown, costEstimate, durationEstimate, whyRecommended,
missingData, estimatedFields, tags, riskFactors, legs
```

固定归一化、成本与 P50/P90 时效 fallback、未知风险处理和前端示例见 `docs/route_recommendation.md`。`legs.from` 和 `legs.to` 继续包含名称、城市、国家、坐标和坐标来源；每段还返回 geometry、可行性、空间暴露、Provider 风险以及分段成本/时效估算。

### 5.5 新闻聚类与路线天气

| 接口 | 主要参数 | 用途 |
|---|---|---|
| `GET /api/risk/news` | `zone_id`, `limit` | 原始 GDELT 文章、类别与事件簇 |
| `GET /api/risk/news/zones` | 无 | 区域风险、文章/事件簇数和 TTL |
| `GET /api/risk/news/clusters` | `zone_id`, `category`, `active_only`, `limit` | 去重后的事件簇 |
| `GET /api/routes/weather-risks` | `mode`, `status`, `active_only`, `limit` | 路线天气风险列表 |
| `GET /api/routes/weather-risks/{segment_id}` | 路段 ID | 采样点、预报时刻和风险因子 |
| `POST /api/admin/gdelt/update` | 管理员 Token | 后台触发 GDELT 更新 |
| `POST /api/admin/weather/routes/update` | 管理员 Token | 后台触发路线天气更新 |

### 5.6 坐标、几何与风险区

| 接口 | 主要参数 | 用途 |
|---|---|---|
| `GET /api/geography/locations` | `status`, `limit` | 返回坐标、来源、许可证、状态与置信度 |
| `GET /api/geography/zones` | `include_geometry` | 返回风险区 GeoJSON、来源和当前 GDELT 风险 |
| `GET /api/geography/segments/{segment_id}` | 路段 ID | 返回路线 GeoJSON、可行性与 `PASSES_THROUGH` 证据 |

### 5.7 AIS 港口流量

| 接口 | 主要参数 | 用途 |
|---|---|---|
| `GET /api/ais/targets` | 无 | 四个 AIS 监测区域、边界框和最新快照 |
| `GET /api/ais/targets/{target_id}/traffic` | `port-shanghai`、`port-singapore`、`port-rotterdam`、`suez-canal` | 港区或走廊最新窗口聚合 |
| `GET /api/ports/{port_id}/traffic` | `CN-SHA`、`SGSIN`、`NLRTM` | 港口信息与最新真实 AIS 流量；无快照时 `traffic=null` |
| `GET /api/vessels/{mmsi}` | 9 位 MMSI | 船舶最新位置与可获得的静态资料，缺失字段为 `null` |

## 6. 当前缺口与建议

1. **AIS 代码已完成，但真实 worker 尚未配置**：当前 `.env` 没有 `AISSTREAM_API_KEY`，AuraDB 还没有真实 `PortTrafficSnapshot`。
2. **需要独立常驻 worker**：Render FastAPI Web Service 不会自动运行 AIS WebSocket；应创建 Background Worker 或在其他持续在线服务器运行。
3. **AISStream.io 是 Beta 服务**：没有 SLA，前端必须展示 `healthy/degraded/unavailable/stale`，后续可接第二 AIS Provider 做冗余。
4. **高频历史轨迹尚未进入时序库**：当前按设计只保留最新位置和窗口聚合；需要完整轨迹时，应实现 `AisStorage` 的 TimescaleDB/PostgreSQL 版本。
5. **地点总体坐标仍不完整**：按审计涉及的全部旧/新地点标签统计为 89/147；`TransportLocation` 为 89/102。前端必须检查 `coordinateStatus` 与 `coordinateConfidence`。
6. **铁路几何 Provider 缺失**：当前铁路仅有端点推断，不参与可信几何交叉；后续应接入铁路网络和跨境运营可行性数据。
7. **旧路线模式未完全规范化**：535/613 个分段仍使用 `multimodal`、`delivery`、`truck` 等旧值；新推荐层会规范化或过滤，但数据库仍需分批重构。
8. **21 个路段端点关系异常**：缺少或重复 `FROM_NODE/TO_NODE`，应先修复关系再扩大推荐覆盖。
9. **运输 Provider 和真实班期缺失**：`RouteSegment.provider` 仍为 0/613，现有成本和时效主要用于估算比较，不能称为报价或承诺时效。
10. **供应商接驳链未达到目标模型**：25/29 个供应商有旧 `SHIPS_FROM`，但 `SHIPS_FROM→设施→HAS_ACCESS_LEG` 完整链仍为 0/29。

## 7. 建议的 AIS 风险评估链路

```text
AISStream PositionReport / ShipStaticData
  -> 去重、异常过滤、静态资料与位置合并
  -> Vessel + 每船一个最新 VesselObservation
  -> AisObservationTarget 时间窗口聚合
  -> PortTrafficSnapshot
  -> Port 端点或 GeoZone/PASSES_THROUGH 路段匹配
  -> 更新 RouteSegment.ais_congestion_score
  -> 重算 RouteSegment.provider_risk_score
  -> /api/routes/recommend 返回最新路线风险
```

不指定 MMSI 时，系统按港区边界框聚合唯一船数、平均速度、低速/锚泊船比例以及到离港变化。只有窗口存在真实位置观测时才生成快照；过期后自动退出推荐。当前阶段没有把单船 AIS 自动绑定到运单，因为数据库缺少可验证的运单与船舶分配 Provider。
